export enum TaskStatus {
  TODO = "TODO",
  IN_PROGRESS = "IN_PROGRESS",
  DONE = "DONE",
  CANCEL = "CANCEL"
}

export enum TaskType {
  STUDY = "STUDY",
  CREATE = "CREATE",
  REVIEW = "REVIEW",
  CORRECT = "CORRECT",
}